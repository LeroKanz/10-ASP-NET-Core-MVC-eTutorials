@model string;

@if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
{
    <div style="position:fixed; right:70px; bottom:70px" class="text-white-70">
        <a asp-action="Create" asp-controller="@Model" class="btn btn-success"><i class="bi bi-plus-circle"></i> Add </a>
    </div>
}
